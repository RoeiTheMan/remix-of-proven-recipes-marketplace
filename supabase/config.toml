project_id = "wvvklhszudwlsmplngcg"

# The Advisor is a public marketplace page. The function applies its own
# validation and best-effort rate limit, and only reads published listings.
[functions.advisor-recommend]
verify_jwt = false

[functions.advisor-transcribe]
verify_jwt = false

[functions.advisor-speak]
verify_jwt = false

# Transactional purchase emails via Resend. The function verifies the caller's
# JWT itself (auth/v1/user) and confirms they own the purchase before sending,
# so platform-level JWT verification is left off to match the advisor pattern.
[functions.send-purchase-emails]
verify_jwt = false
